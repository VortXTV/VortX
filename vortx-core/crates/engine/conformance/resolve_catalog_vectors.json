{
  "description": "Cross-language conformance for the engine catalog resolution query. With the default owner profile (not a kids profile, no ceiling), the catalog is a passthrough: every row is visible regardless of its certification. The kids-filtering path depends on engine state (the active profile's parental flags) and is proven by the integration + property tests, which set up a kids profile through the dispatch path first.",
  "cases": [
    {
      "name": "owner_profile_sees_every_row",
      "request": {
        "kind": "catalog",
        "metas": [
          { "id": "a", "type": "movie", "name": "A", "certification": "R" },
          { "id": "b", "type": "movie", "name": "B" },
          { "id": "c", "type": "movie", "name": "C", "certification": "G" }
        ]
      },
      "expect_ids": ["a", "b", "c"]
    }
  ]
}
