{
  "description": "Cross-language conformance for the engine meta resolution query. With the default owner profile (not kids), a meta is always returned. The kids deep-link block depends on engine state (the active profile's parental flags) and is proven by the integration + property tests.",
  "cases": [
    {
      "name": "owner_profile_gets_the_meta",
      "request": {
        "kind": "meta",
        "meta": { "id": "tt1", "type": "movie", "name": "A", "certification": "R" }
      },
      "expect_id": "tt1"
    }
  ]
}
