# Cloudflare Workers / wrangler
.wrangler/
node_modules/
dist/
# local secrets (real secrets are set with `wrangler secret put`, never committed)
.dev.vars
.dev.vars.*
.env
.env.*
# wrangler.toml holds the real D1 database_id; keep it local and commit wrangler.toml.example instead
wrangler.toml
# agent/editor config
.claude/
